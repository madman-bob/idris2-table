. ../testutils.sh

basicTest ExampleTables.idr << EOF
    showFrame employees
    showFrame moreEmployees

    showFrame {schema = EmployeeSchema} [<]
    showFrame $ employees :< robinson

    showFrame $ init employees
    last employees

    showFrame $ frame $ the (Table EmployeeSchema) [<robinson, smith, williams]
    showTable $ table employees

    rowFromEnd employees 0
EOF
